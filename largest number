import java.util.*;

public class Main {
    public static void main(String[] args) {
      int arr[]={1,99,2,3,4,5};
      int largest = 0;
      for(int num:arr) {
        if (num > largest)
        largest = num;
      }
      System.out.println("lar:" + largest);
  }
}
